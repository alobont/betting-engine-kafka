# Progress Notes

Use this file as a short implementation log.

Entry format:
- `YYYY-MM-DD HH:MM` - what changed, why, and how it was verified

Notes:
- keep entries short
- record blockers and assumptions explicitly
- record test or verification commands when relevant
- use `LESSONS_LEARNED.md` for generalized takeaways rather than repeating them here

- `2026-03-11 22:53` - Updated `.ai/SPEC_CHECKLIST.md` to align with Protobuf internal messaging, local `kind` Kubernetes integration testing, and explicit end-to-end cluster execution. Added root `README.md` documenting one-line local cluster startup, test workflow, namespace cleanup, and full cluster teardown for Windows PowerShell and Linux.
- `2026-03-11 23:50` - Implemented the Gradle-based Spring Boot service, Protobuf messaging flow, H2 seed data, Dockerfile, `kind` Kubernetes manifests, Gradle local-cluster tasks, and cluster-facing integration/e2e tests. Verification is in progress, starting with test isolation fixes and then the full local Kubernetes flow.
- `2026-03-12 00:30` - Completed verification. Ran `.\gradlew.bat integrationTest --no-daemon`, `.\gradlew.bat test e2eTest --no-daemon`, and `.\gradlew.bat test --rerun-tasks --no-daemon` with `JAVA_HOME` set to Java 21 and `KIND_BIN` set to the local `kind.exe`. The local `kind` deployment handled the full HTTP -> Kafka -> bet lookup -> RocketMQ flow through `nginx`, and the checklist is now fully complete.
- `2026-03-12 10:47` - Reworked the follow-up architecture toward Redis as the in-memory bet store and cluster-wide idempotency backend. Added `docs/AGENTS_REDIS_READY.md` as a copy-paste-ready replacement body for `AGENTS.md` and updated `.ai/SPEC_CHECKLIST.md` so Redis, dedicated Redis deployment, and process-safe local locking are now the outstanding implementation targets.
- `2026-03-12 12:52` - Replaced H2/JPA with Redis-backed bet storage and Redis settlement claims, removed the local duplicate guard from the correctness path, added a dedicated Redis deployment/service plus Redis wait wiring in Kubernetes, and updated the root `README.md` for the Redis-backed local workflow. Verified with `.\gradlew.bat test --no-daemon`, `.\gradlew.bat integrationTest --no-daemon`, and `.\gradlew.bat e2eTest --no-daemon` using Java 21 and the local `kind.exe`; the deployed stack passed HTTP -> Kafka -> Redis bet lookup/idempotency -> RocketMQ through `nginx`.
- `2026-03-12 13:11` - Renamed the Java and generated Protobuf package root from `com.sporty.bettingengine` to `com.bettingengine`, moved the source/test directory tree to match, updated Gradle coordinates, and removed remaining `com.sporty` references from tracked text files. Verified with `.\gradlew.bat localK8sVerify --no-daemon` using Java 21 and the local `kind.exe`.
- `2026-03-12 13:23` - Added Gradle-managed `kind` bootstrapping so `localClusterUp` now downloads the pinned platform-specific binary into `.tools/` when missing and uses it by default. Also updated `README.md` to document the managed tool flow and optional overrides.
- `2026-03-12 16:48` - Added dual local Kubernetes modes: default non-HA and opt-in HA with `-Plocal.ha=true`. Implemented HA manifests for Kafka KRaft (`3` nodes), Redis server plus Sentinel (`3 + 3`), RocketMQ (`2` nameservers with `2` sync-master / `2` slave brokers), and `2` replicas each for `betting-engine` and `nginx`. Hardened the local app rollout with startup probes, smaller JVM sizing, and clean Kafka/Redis refresh during `localClusterUp`.
- `2026-03-12 16:49` - Extended the integration test support for explicit `kubectl` context selection, multi-pod log aggregation, statefulset readiness, Redis Sentinel master detection, and pod-recovery helpers. Added HA integration coverage and updated state-reset behavior so test runs do not leak Redis or Kafka state across scenarios.
- `2026-03-12 16:50` - Verified both local modes with Java 21. Non-HA verification passed with `.\gradlew.bat localK8sVerify --no-daemon`. HA verification passed with `.\gradlew.bat localClusterUp --no-daemon '-Plocal.ha=true'` followed by `.\gradlew.bat integrationTest e2eTest --no-daemon '-Plocal.ha=true' -x localClusterUp`.
- `2026-03-12 17:07` - Updated the `.ai` working docs to align with the Ignite-based AGENTS direction. Rewrote `.ai/SPEC_CHECKLIST.md` so Redis-specific items are replaced with Ignite requirements and reopened where the current implementation no longer satisfies the architecture, and added a reminder in `.ai/README.md` that the checklist must track the current AGENTS architecture before further implementation proceeds.
- `2026-03-13 02:20` - Replaced the Redis runtime with Apache Ignite across Spring Boot configuration, repositories, seed data, Kubernetes manifests, and local integration support. Verified the Ignite-backed non-HA flow with `.\gradlew.bat integrationTest --no-daemon` and `.\gradlew.bat e2eTest --no-daemon` using Java 21. Also reworked HA infrastructure toward Ignite plus RocketMQ stateful nameservers and a one-pod-first betting-engine bootstrap, but full HA verification remains incomplete because the local kind HA cluster has shown intermittent Kubernetes API TLS handshake timeouts and very slow application cold starts under full load.
- `2026-03-13 05:27` - Finished the Redis-to-Ignite implementation and verification. Hardened the local image build to package the host-built `bootJar`, fixed HA Ignite bootstrap polling, fixed HA RocketMQ topic creation by quoting the semicolon-delimited nameserver list, corrected HA test assumptions around the nameserver statefulset, and added stronger HA Ignite duplicate-protection coverage. Verified with `.\gradlew.bat localK8sVerify --no-daemon`, `.\gradlew.bat localClusterUp --no-daemon '-Plocal.ha=true'`, and `.\gradlew.bat integrationTest e2eTest --no-daemon '-Plocal.ha=true' -x localClusterUp`.
- `2026-03-13 10:25` - Refactored the repositories further toward Ignite table APIs: `BetRepository` now uses an Ignite criteria query instead of raw SQL for `findByEventId`, and `SettlementClaimRepository` now uses `KeyValueView` CAS operations for settlement claims. Also excluded `.tmp/` from the Docker build context after it interfered with local image packaging. Verified with `.\gradlew.bat test --tests com.bettingengine.repository.BetRepositoryTest --tests com.bettingengine.repository.SettlementClaimRepositoryTest --no-daemon`, `.\gradlew.bat test --no-daemon`, `.\gradlew.bat localK8sVerify --no-daemon`, `.\gradlew.bat localClusterUp --no-daemon '-Plocal.ha=true'`, and `.\gradlew.bat integrationTest e2eTest --no-daemon '-Plocal.ha=true' -x localClusterUp`.
